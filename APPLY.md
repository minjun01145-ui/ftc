# FTC 체험학습 일정 기능 개선 패치

기준 저장소: `minjun01145-ui/ftc` main, 기준 커밋 `a1d6c54a4609df7eae24eaab7d5de2e4098e797c`

## 적용 내용

- 사업을 선택하거나 새 사업을 만든 직후 `전체보기` 대신 `사업정보`로 진입
- 사업정보 아래 `체험학습 일정 입력` 영역 추가
- PDF/JPG 복수 파일 선택 및 형식 검증 UI 추가
- 실제 파일/AI 분석 호출은 구현하지 않음
- AI가 나중에 채울 `tripSchedule.items` 표준 모델 추가
- 일정은 기본 읽기 전용 미리보기이며 `수정` 후 편집 가능, `저장`으로 저장
- 일정 저장 시 학생용 `체험처/비용` 행을 자동 동기화
- 일정에서 넘어온 날짜/체험처/세부정보만 갱신하고 단가·계산방법·실지출액 등 기존 비용 입력값은 보존
- 사용자가 수동 추가한 비용 행은 일정 재저장 시 보존
- 기존 저장 데이터 호환을 위해 schemaVersion 4 정규화 추가

## 패치 적용

저장소 루트에서:

```bash
patch -p1 < ftc-trip-schedule.patch
npm test
```

또는 ZIP 안의 `js/`, `tests/` 파일을 저장소의 같은 경로에 덮어쓰면 됩니다.

## 테스트

로컬 구성에서 관련 기존 모델 테스트와 신규 일정 테스트 총 12개가 통과했습니다.
