# 개발 가이드

이 문서는 기능을 더 붙일 때 코드가 한 파일로 몰리지 않게 하기 위한 최소 규칙만 정리합니다. 현재 구조를 다시 설계하기 위한 문서가 아닙니다.

## 모듈 경계

### `js/engine.js`

DOM, localStorage, 네트워크와 무관한 계산 규칙을 둡니다.

예: 학생/인솔자 비용 계산, 지원금 배분, 검증.

계산식을 화면 파일에 새로 만들지 말고 여기의 함수로 만들고 테스트를 붙입니다.

### `js/views/`

HTML 생성, 폼 값 읽기, DOM 조작을 담당합니다.

뷰에서 계산 규칙을 직접 구현하지 않습니다. 계산 결과가 필요하면 `engine.js`를 호출합니다.

### `js/app.js`

화면 전환과 상위 이벤트 연결을 담당합니다.

새 기능이 여러 단계의 처리나 외부 통신을 필요로 하면 모든 로직을 `app.js`의 `click` 분기에 넣지 말고 별도 서비스/컨트롤러 모듈로 분리합니다. 특히 AI, 파일 처리, 외부 API 기능은 이 원칙을 우선 적용합니다.

### `js/presets.js`

저장 데이터의 기본 구조와 정규화를 담당합니다.

새 저장 필드를 추가할 때는 기본값과 이전 데이터 보완 규칙을 같이 추가합니다. 스키마 변경 뒤에는 `tests/model.test.mjs`에 호환 테스트를 추가합니다.

### `js/services/`

학교알리미처럼 외부 시스템과 통신하는 코드를 둡니다. 화면이 외부 API 형식을 직접 알지 않게 합니다.

### `js/ai/`, `functions/src/ai/`

AI는 기존 계산의 기준값을 대신하지 않습니다. `engine.js`가 만든 구조화된 값의 설명, 추천, 문서화 같은 보조 기능으로 붙이는 것을 기본 원칙으로 합니다.

## `staffExpenses` 주의점

학생용 `expenses`와 인솔자 전용 `staffExpenses`는 독립적으로 저장됩니다.

현재 학생 재원 배분과 요약의 기준인 `allocateFunding()`은 `expenses`만 사용합니다. 학생용 항목의 `sharedFixed` 계산에서 발생한 인솔자 몫은 기존 요약에 포함되지만, `staffExpenses`의 독립 항목은 자동 합산되지 않습니다.

따라서 앞으로 “전체 행사비”, “인솔자 예산”, “학교 회계 정산” 기능을 만들 때는 `staffExpenses`를 어떤 합계에 포함할지 먼저 요구사항을 정한 뒤 엔진에 명시적인 함수를 추가합니다. UI에서 임의로 합산하지 않습니다.

## 파생값

현재 저장 모델에는 호환성 때문에 `actualParticipants`, `vulnerableParticipants` 같은 파생값도 존재합니다. 새 코드에서 비슷한 파생 필드를 추가하기 전에 원본 값으로 계산할 수 있는지 먼저 확인합니다.

기존 필드를 바로 삭제하거나 저장 형식을 바꾸지는 않습니다. 데이터 마이그레이션이 필요할 때 별도로 정리합니다.

## 리팩터링 시점

파일 길이만 보고 미리 나누지 않습니다. 다음 중 하나가 발생하면 분리를 고려합니다.

- 서로 다른 업무 규칙이 한 파일에서 함께 변경된다.
- DOM 코드와 계산/통신 코드가 섞인다.
- 같은 계산이 두 곳 이상에서 반복된다.
- 테스트하려면 브라우저 DOM 전체가 필요한 순수 로직이 생긴다.

현재 우선 감시 대상은 `js/app.js`와 `js/views/expenseTable.js`입니다. 새 기능을 추가할 때 이 두 파일에 책임이 계속 쌓이지 않도록 합니다.
